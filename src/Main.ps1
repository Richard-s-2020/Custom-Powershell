#Import DataBase
$Timer = Get-Date -Format "HH:mm"
.'.\src\Modules\Set-up DataBase.ps1' -jsonFilePath .\DataBase\main.json

# $return moet 
#Ouput: ->Debug(Debug)  -> software| voor set-up Tekst -> install allen voor install